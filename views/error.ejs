<%- include('partials/head') %>
<% if (currentUser) { %><%- include('partials/nav') %><% } else { %>
<div class="shell"><div class="content" style="margin-left:0"><main>
<% } %>

<div class="card" style="max-width:520px;margin:48px auto;text-align:center;padding:38px">
  <div style="display:flex;justify-content:center;margin-bottom:16px">
    <%- include('partials/logo', { size: 46 }) %>
  </div>
  <h1><%= title %></h1>
  <p class="muted" style="margin:10px 0 22px"><%= message %></p>
  <a class="btn primary" href="/" style="display:inline-flex">Back to dashboard</a>
</div>

<% if (currentUser) { %><%- include('partials/foot') %><% } else { %>
</main></div></div></body></html>
<% } %>
